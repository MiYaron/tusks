import { GroupTasksPipe } from './group-tasks.pipe';

describe('GroupTasksPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupTasksPipe();
    expect(pipe).toBeTruthy();
  });
});
