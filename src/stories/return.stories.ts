import { Meta, StoryObj } from '@storybook/angular';
import { ReturnButtonComponent } from '../app/components/return-button/return-button.component';

const meta: Meta<ReturnButtonComponent> = {
    title: 'Components/Return Button',
    component: ReturnButtonComponent,
  };

export default meta;
type Story = StoryObj<ReturnButtonComponent>;

export const button: Story = {
};